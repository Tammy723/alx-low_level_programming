#include "main.h"

/**
 * puts2 - a function that prints every other character of a string
 * @str: string to printed
 * Return: 0
 */

void puts2(char *str)
{
	char *srt;

	str = "0123456789";
	puts2(str);
	return (0);
}
